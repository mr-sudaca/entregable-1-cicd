name: "Mi primer ci workflow"

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  imprimir_info:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3 
      - name: 'Imprimir Mensaje Personalizado'
        run: |
          echo "Hola, somos Jhon Henry Quintero Cárdenas y Carolina Rangel Sanchez y este es nuestro primer workflow!"  
          echo "La fecha y hora actual es: $(date)"  

      - name: Configurar Python
        uses: actions/setup-python@v3
        with:
          python-version: '3.12'

      - name: Ejecutar script Python
        run: 'python mi_script.py'
